import classNames from "classnames";
import styles from "./styles.module.scss";
import { RefObject } from "react";

type Props = { ref: RefObject };

export default function Form2({ ref }: Props) {
  return <>Form2</>;
}
